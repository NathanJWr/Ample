func foo2 (var1, var2) {

}

func foo (var) {
  a = var;
}

func abc() {
  var = 20;
}

# functions can take multiple arguments
foo2 ("Hello", "World!");

# calling this function requires no arguments
abc();

# can call foo with different argument types
foo("Hey this is in an argument!");
foo(10);


